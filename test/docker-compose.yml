version: "3.9"

services:
  mosquitto:
    image: eclipse-mosquitto:2.0.12
    ports:
      - "8883:8883"
    volumes:
      - ./mosquitto:/mosquitto/config
  db:
    image: mongo:3.2
    ports:
      - "27018:27017"
